package com.black.xperiments.graphviewrecyclerview.fakesensor

interface SensorStream {
    fun dataA(signal:Double)
    fun dataB(signal:Double)
    fun dataC(signal:Double)
    fun dataD(signal:Double)
    fun dataE(signal:Double)
}